

void eepromBegin() {

  /*Kp = (float)EEPROM.read(eP) / 10;
    Ki = (float)EEPROM.read(eI) / 10;
    Kd = (float)EEPROM.read(eD) / 10;

    BaseSpeed = (int)EEPROM.read(eBase);
    MaxSpeed = (int)EEPROM.read(eMax);
    sPrint =  (int)EEPROM.read(ePrint);
  */

  /*BaseSpeed = 100;
  MaxSpeed =100;*/
  sPrint =  1;

}

