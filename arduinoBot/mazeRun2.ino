
void mazeRunAdvanced() {

 /*//SENSOR NUMBERS SHOULD BE CHANGED!
  int frontDist = getDist(0);
  int leftDist = getDist(1);
  int rightDist = getDist(2);

  if (isMazeSolved == 0) {
    //left  (0,-1)
    //right (0,1)
    //front (1,0)
    //back  (-1,0)

    if (rightDist > maze_THERSOLD_FOR_WALL) {
      maze_turnRight();
      int tempdir[][] = {dir[2], dir[3], dir[1], dir[0]};
      dir = tempdir;
    }
    else if (frontDist > maze_THERSOLD_FOR_WALL) {
      maze_goForward();
    }
    else if (leftDist > maze_THERSOLD_FOR_WALL) {
      maze_turnLeft();
      int tempdir[][] = {dir[3], dir[2], dir[0], dir[1]};
      dir = tempdir;
    }
    else {
      maze_turnBack();
      int tempdir[][] = {dir[1], dir[0], dir[3], dir[2]};
      dir = tempdir;
    }

    // go forward
    posX += dir[2][0];
    posY += dir[2][1];

  } else {
    //
  }
*/






}

